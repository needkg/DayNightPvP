package com.needkg.daynightpvp.command;

import org.bukkit.command.PluginCommand;
import org.bukkit.plugin.java.JavaPlugin;

public class Ahh {

    private final JavaPlugin plugin;

    public Ahh(JavaPlugin plugin) {
        this.plugin = plugin;
    }

    void aham() {
        // PluginCommand command = plugin.getCommand("aham");
        // if (command != null) {
        //     command.setExecutor(plugin);
        //     command.setExecutor(new AhhCommandExecutor());
        // }
    }

}
